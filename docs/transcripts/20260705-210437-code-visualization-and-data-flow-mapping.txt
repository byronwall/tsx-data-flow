Alright, here we go. This first one's going to be about code visualization. And specifically a couple ideas. One is how can you hop into a new code base and figure out the general way of what is going on. So very very high level code-based visualization. And the goal there is going to be some part architecture, some part technology, some part just on disk file layout. And the common scenario is you're reviewing someone else's work, you're popping into a new project, you are picking up one of your own projects from a while ago, and you just want to get the 10,000 foot view. Related to that is at the start of a project, even before the code is actually written, you might want to kind of pre-visualize what would the architecture look like before the code's been written. If you looked at enough diagrams, if you've looked at enough maps of here's what a good application looks like, you could probably look at a proposed architecture and say if this is what the code this is what the code's diagram will look like, then there's a reasonable chance that the code is correct or could be correct if it adhered to the diagram. The next reason is going to be making sense of changes. So if you've got a architectural diagram, it provides this map of tier is where everything is located, which then allows you to say, given that I want to implement this feature. What all does it cut through? And this sort of gets into the world of horizontal versus vertical slices. If you think of the true back end all the way down to the true front end for web app, where the front end is the actual user visible stuff, and the back end is everything that comes before that. Then the vertical slice is how can we go from the very tippy top of the back end all the way to the front end and actually get something renderable. A horizontal slice is going to be able to make that thing work robustly or at scale. What other systems need to be in place to support each node in the chain from back to front. Very often you can get a crappy but working version of something in a thin vertical slice. Or what you originally proposed is not actually what you want. Or you've got a hundred details to propose at the start. Once there's 15 that change the vertical slice, once you catch those 15, or maybe you catch 13 of them, you can reroute those to something different. The nice thing is maybe in the 13 of 15 that you've changed, you've now lessened or modified the requirements on the back end or some other spot in the chain. And so the vertical slice lets you see once you see this before you've added a whole bunch of code that now needs to get reworked again. So if you've got a good visualization of a code base at the big ticket level, you can look at a change and say, how narrowly focused is this. Or you might say, oh yeah, that makes sense. Or you might say, well, we will eventually need to modify the back end thing, but in the beginning, let's just make it look like this so that we can go implement it in the front end only and get a feel for how it would behave. What else does a good code-based visualizer give you? Big picture, it lets you see just the general shape and trajectory of data as it flows through your system. So this is getting into a little bit more granular. But if ultimately you've got some amount of user input, goes into a database, goes through some amount of processing steps, eventually comes out through endpoints into your front end. Maybe there's some statefulness on the front end, and then ultimately the thing renders, and then after it renders, you're collecting user input interactivity, you're working against the data. You're then mutating, manipulating generally wanting to make changes on the data, those changes go back into the back end. And now you've kind of closed the loop on back end to front end to back end and back. And once you've closed that loop you've got a nice coherent system that is hopefully solving some specific problem for the user. But if you really have that chain of input goes to post processing goes to transformation goes to additional transformation goes to the all those intermediate steps you might then need to reason about well if we're gonna allow the user to mutate something what are they mutating where in that chain are they actually making the change for anything that's downstream of that change propagate it for anything that's upstream of that change do we need to handle that differently the cleanest scenario that basically never happens is that the user is directly modifying their original input and you just rerun the whole pipeline again pretty quickly you end up in a spot where that's not feasible not desirable and so you're overlaying these changes on top of some intermediate state and having a good visual can show you exactly where all those choices are being made. My suspicion is that is, especially in this world of agents and code you're not checking as thoroughly, those sorts of visualizations can probably really help pen down where is complexity and edge cases and errors being introduced. Because the other pressing detail is when you're building a new system, you're often making large sweeping changes across a bunch of different areas. And so pretty quickly a feature that you thought was critical is no longer necessary. Okay, so that idea of code mapping to a feature. This gets to probably one of the key requirements, expectations, goals of code-based visualization, is that you're mapping the units that people think and talk about onto the code that actually does it. So for example, uh I've got this web page capture thing. And so there's this whole pipeline that's doing the capturing. Downstream of the capturing, it's then using DOM information to annotate this section of the web page is likely a pricing component. And so that whole pipeline. And if I'm being honest, I don't even know what some of those intermediate data structures look like. And so a visualizer could probably make very real. It could reveal those intermediate steps. And in particular, show the places where raw data is being transformed to something to something to something. And especially it might be able to show the places where some code path that used to exist required some chunk of data that used to be important. But hey, now given that everything renders this other way over here, we can go get rid of that whole code path. Which is to say code evolves over time, especially during MVP work. It's evolving rapidly. Things are coming, things are going, things are being fixed. An edge case that used to exist gets removed via some other means, but there's still some code or some residual data in place. And then all of those are the sorts of things that become landmines in the future because the next agent doesn't know that that code's irrelevant. And so now they're doing some work to preserve a chunk of data structure that's just not needed anymore. And so all that on necessity is to say maybe could go look at the actual user-facing functionality. Think about the data structures that are needed to actually make that happen. Or the transformations only include what's strictly necessary and don't include any of the vestigial steps that have been removed. So I think all that is starting to get in the world of more of like a data flow analysis. And I've got that analysis tool, which is meant to go from the actual uh sincs in the TSX files. So what are the actual things we're rendering? Show the path, I guess, from the hair all the way back. Um I'd be curious what that looks like on some of these uh GPT written code bases because my suspicion is that just looking at that flow back and in particular looking at the data structures along the way. There's probably a ton of just gratuitous transformation and processing being done, and a good uh network diagram just showing how information flows would probably lay bare. Oh, this could be cleaned up. Um also once you start mapping all of the individual data flows, it's very very easy to create a giant spaghetti diagram where in trying to see everything, you can basically see nothing again. And so there's probably a sense of you've gotta go in for a specific page for some specific aspect of that page and just see what complexity exists. Um possible, and I guess what I'm describing is you've got the code itself, so how the functions call each other. You've got the actual data structures that are expected. I think in type language like TypeScript makes it way easier to see what's going on. Um basically see, yeah, you can almost see the chain of calls as you saw the raw data is getting transformed into something else. And you'd see all these small dumb little one-off steps where something's being mutated in place, or something is being cast to some other random thing, just to be cast back a call later, and so it's almost like a uh data flow graph that's independent of the functions that are doing it, or instead of showing hey, here's a function that takes these arguments, you basically just say uh here's the data in the database. It's like you're taking snapshots of the major data structures. So, yeah, there's a function that returns uh is parsable or something, but at the end of the day, you've got these large objects flowing through, they have some shape, and to the extent that shape is changing at every call site. That's probably a hint that there's some missing normalization or rounding upstream. For what it's worth, the uh all TypeScript code bases, front and back in one language, could actually make it way easier to see that because things are going over the wire, basically in their type form. And so it should be very easy to do static analysis all the way back to the database or the user input. I think that gets to the next thing that would be useful, which is to separate the initial what are the raw bits of data coming in. You could say, well, what's the raw bit of information coming in? I've got a URL. I know I'm on a URL, I capture it. Um if If I had a system that could just take a URL and capture it, technically that's all I need. My only input is the URL. But the honest truth is browsers are not good enough. You just start with the URL to capture everything. And so what am I really doing? My starting input is a Chrome session with this extension running. The extension is running through this complicated capture process, generating these complicated data structures, these complicated images. Is there a link back to it? It takes that whole mess of stuff. And that initial pile is technically the input to my system. And then from there, I'm doing all my work on that. And I guess even just seeing that would maybe hint that like was there an even just simpler version of that data structure? Can I get my input to be in the cleanest most useful least additional steps required to work with it? Because if that's true, there's probably some step that's taking my raw database and then turning it into something useful. Yeah, and if you have to like make a chart of that, it's like how useful is this data structure from the point of view of the components that are rendering at the end of the day. And you're at 100%. Once you finally render the thing in the front end, you're at 0% on the input side. And so it's like every call in the chain should in theory be taking that thing from zero to 100%. And the absolute cleanest, the simplest approach is that the thing you store in the database is the thing you render. On the front end, you just directly render it. Is that true? No, because probably on the front end, you would actually need to. I guess you can just render it as tax. Okay, your renderer is stacked. That's not useful though. More likely you're gonna split the thing into columns. And so then what could be the most useful thing to store on the back end? Uh some JSON object, which is the rows and columns. Would that be it? Maybe. And okay, we can render this thing. Which is good. But then we get to the next problem, which is we actually want to support uh sorting and filtering. And we want the filtering to have a nice drop-down of all the available choices. And so what do we have to do to make that work? Well, we need to go through the data per column, find the unique items, and then render those lists. Okay. So what would have made that easier? Well, I need this list of unique items. I'm deriving it from the column of data. That's fine. But also could the server have just sent me a list of unique items? Yeah, probably. How would the server get a list of unique items? Uh it can be some on demand every time, maybe. Okay. How else could the server get a list of unique items? It could just maintain a list of unique items. And every time new data comes into the system, it updates it. And so it's just always correct. You don't have to do any extra work. That's a kind of in the world of well, you could let the database just do that. You're basically doing the same thing. But now you'll let the database do the heavy lifting there. So that would work. But I've got numeric data. Um I've got dates. I want to show a nice date filtering component, but I only want to offer selectors the dates that actually exist. And so now you're in this dual of you know all possible dates. That's just a calendar. But you want to convey some sort of visual of these are the dates that actually exist. And you want to do that at the one probably the year level, the day level. That's an app. You're doing a bunch of processing. To make it so that you can go render that component at the very end. Again, what's the raw source for that thing? This is interesting. It's like not strictly a user input. It's not like the user said, hey, we tell you in advance that these are all the dates that are available. No, they're giving you some other data, which is in their mind the actual data. And then you're having to go derive compute all these additional things off of that data. So you either have to choose to store it and keep it up to date, or take the compute hit of regenerating it every time. Component. All right, so maybe that uh useful enough on the table data thing. Let's get back to the uh example of the DOM capture thing. Because what's going on there is much gnarlier, which is that there's a bunch of intermediate processing steps being done to keep track of all the coordinate spaces, the sizes, the styles. logic about things that were technically captured that should not be displayed at a given time because the capture is this whole full page scrolling rendering and it's trying to deal with sticky stuff and fixed stuff and who knows what other stuff and at the end of the day it's trying to show subsets of that content that are still pixel perfect and that's still maintaining the link back to the original DOM elements. That way I can then show logic around font and color and background and sizes. It's all kind of an illusion because I'm just rendering it on top of a static image. And so we've got all these subset image things going on. And I guess this gets to what would be a very useful visualization in that system which is it generating intermediate images that are correct or is it basically maintaining this giant chain of calculations all the way through. I'm pretty sure it's a giant chain of calculations which is why there's so many bugs. And so now that we know the sorts of renderings that we expect to be doing maybe it would make sense to go through and have the thing generate some intermediate snapshots, intermediate screenshots that are just known correct and now you've got this much easier to verify chain of we've got the raw stuff. We've got the snippets that we're working with. And we're not having to reproduce the full calculation logic just to render a small footer area. But then ultimately, one of the core visualizations in the actual product is showing the full capture on an ethnic canvas. And so even though I could go and do the intermediate things, I'm still left with this problem of some of the time I need to render everything and still have it be correct. And so to the extent I can faithfully render everything and have it be correct. Is it so much worse to render a subset? The answer might be yeah. Yeah, maybe it is. If what I'm doing is rendering the whole thing and then only taking a small slice of it, well, that may not be very good. I think it's actually got a huge pile of code that's way more complicated than that though. I think what's actually doing is rendering just what's needed by virtue of doing all the viewport and intersection and other math. But this is where a good visualizer would be helpful. What else? Seeing how the data structures weave their way through the front end would also be very useful. This is one of those issues that plagued the model art project, which is that we would start with some clean entity data structure on first transfer, but then by the time it's been rendered, it's gone through five or six layers of transformations. And so you're having to do a ton of work just to pin it back down to the entity it started as over in that world. There was a ton of using the canonical ID of something and then going and getting the object for an ID rather than just carrying the whole object through the chain. That's like at various points in time. We started with the full object, we do some processing, we'd drop some fields, we'd maintain the ID, we didn't come back and have to go use the ID to get those fields on some downstream set. Well, do you actually need the whole chain of everything to have access to the whole object? I don't know, maybe, maybe not. But clearly walking through the chain, losing fields just to bring them back on a subsequent query is a hint that they got dropped too early. Or at least there's some critical path through the system where all those fields would have been useful or necessary. Is that originally the component truly only needed to render that subset of fields because it was four of six fields, and then later we added something else. That something else actually needed all the fields, and so instead of correcting it by passing the whole object through, we corrected it by adding an additional lookup downstream. And so if we had a nice visualization of here's how data is flowing through the system, and when you're switching in and out of various data structures, it might make it clear that that's what's going on. And I I think that I don't know if it's a timeline like left or right or what the exact structure of the thing is, but there is some sort of visual of here's what you started with, here's what you're rendering, and for a given page or component or something, you should definitely be able to show here's the full totality of everything that's needed to get these pixels to change on the front end. So then that gets to the next problem, which is once you layer on interactivity or updates or some aspect where the initial data is getting transformed just based off of what the user now wants to see. You now need to keep track of the statefulness of the thing. And how is the actual data that's available changing in time as the system changes? What are the allowed changes that can be made? Who can make them, and then how does that data ultimately get reconciled back with the original source data when some of the interactive changes actually need to be persisted. So for example, these are my annotation thing. I've got some idea of hey, these DOM elements form a pricing component, where a pricing component is maybe three or four of these pricing card things with a header and a call to action button. And maybe some automated process identified all of the cards and the button, but not the header. This needs to be in the group. Well, how are we representing a group? Because if the original rule was, hey, the group must be a common DOM ancestor. That transit no mess because in some cases you don't actually have a common ancestor. That happened on the Verse documentation page. There's very clearly a header and some cards below it. But they've chosen to render those as sibling nodes. It's enough. Oh, it's actually this DOM node, this DOM node, and those together form a group. But then if I'm not storing like the parent, or I'm not storing like a contiguous, hey, all of these DOM nodes together. Well now I've got this gnarly problem where I'm asking the system to store a list of hey, it's actually these six random DOM nodes are the group. I guess maybe that's fine enough. Like once you decide to store more than one common ancestor DOM node. You just store the list and I get a reconcile the list. But uh who cares to go reconcile the list? If you could do one before, you could do a group of them now. You somehow cobble those DOM nodes together, you find their uh bounding elements, you have crop the source image against those bounding elements, and then boom, that's your thing. Now maybe you choose to take the bounding box around those elements and then go crop it that way. That'd probably give a slightly better result, but maybe not. Maybe maybe during the selection process, now the user really said it's this and this, it's definitely not that. I know it's technically intersecting my thing, but I don't want it in my capture. That's some other thing. Um now we've got like a really really kind of plum mess there, which is the data that was stored originally needs to be really thoroughly reworked and manipulated to support this complicated selection interaction. But then the thing being selected needs to be good and correct for the save as a group interaction. And then somehow you've got to make sure that all that's correct without actually showing the user a full DOM and making them you know type in IDs to this is the group. Um so yeah, it's it's like an actual real gnarly example. And somehow being able to see this is the data that's available. This is what's happening when we're doing the selection. And related to that visual, like it at the very raw input side of things, you can often visualize that, like you know roughly what the user's input might look like. Maybe it's a file, maybe it's a thing you're typing in, maybe it's an image, maybe it's something. The inputs are coming from somewhere, especially if they're coming from a human. There's just kind of a known shape of the structure to that. The outputs kind of have to be a certain way because you're gonna go render them, like eventually you've got pixels that represent the outputs. Uh but somewhere in the middle there is like this shifting transformation from just like the input over to well it's definitely the output, we're at the output, but in between there is like some intermediate transformations that might go through uh three, four, five steps that look completely different because you're having to basically work these intermediate spaces. And if you never see those intermediate spaces on the output, and they're radically different from both the input and the output, well, you're almost like that's part of what you want to visualize, is somehow go visualize those intermediate transformations. Um where you visualize them in the actual product, maybe in some dev tool that can just show you the steps, maybe. Um but if you have this sense of like a static analysis, code-based analyzer that could then attach to the running instance and help you visualize some of these intermediate data structures. That sort of thing might be very useful. Or maybe it's good enough just to point out that you've got all this madness going on in these intermediate data structures, maybe just seeing that they exist, or seeing that there's 30 layers of them, or that there's three, four, five flip and flops, uh, or three, four, five normalization steps where you're basically taking key names and translating them to new key names or something. Maybe just seeing all that and then I was supposed to say, hey, this is uh madness. Let's go clean it up. Like you don't actually have to be able to render the intermediate thing to know that those steps are goofy. But then in other cases you might have some complicated viewport math, and it's not clear at all if it's useful or not. You basically need to see the transformations from raw to final, and once you see them, you can then eyeball oh that's not doing anything useful. That one there is technically, you know, by some scheme it's scaling and rotating and scaling and rotating and scaling and rotating again. But it works out that all of the transform matrices are you know a single rotation only in one specific case, and maybe that case just doesn't matter anymore. Um especially the case with the GPT code. It is so edge casey hopeful and defensive that it often invents the edge case it was trying to save you from. Maybe that's a good spot to end it with, which is just talk about code needing to handle edge cases, but on a lot of these things, it's like what is the edge case? Um actually care about. Is that a thing that needs to be handled today in advance of an example of it being triggered? That's another thing where the GPT code is just really bad. Is inventing all sorts of edge case saving mechanisms that just propagate more edge cases. So being able to pinpoint, oh this is an actual edge case. Oh this is an edge case that exists if this data structure ever comes through. We've actually completely completely removed that data structure. Being able to see some of that would be really good. Alright, we'll stop there.