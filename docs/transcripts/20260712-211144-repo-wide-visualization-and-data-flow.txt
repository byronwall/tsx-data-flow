All right, here we go. This is a recording about desired improvements to the PSX data flow code visualization process. In particular, it's some thoughts, opinions, and expected directions to go with respect to the handful of code visualization and more specifically the repo wide visualizations being done. And since the data flow tool aims to explain data flow through the application, I'd like to somehow get some repo wide visualization of the data flows into this initial view. The big major things I want this view to do are to make it so that someone can be familiar with the various systems and subsystems within the refill. I want the user to have some sense for the connectivity within the repo. For a well-organized repository, it's likely the case that this overall view is going to show a nice zoomed out view of all the different systems and subsystems. We'll see some simple connections at the various boundaries. Individual routes have a shell component or some route component. I don't know, octopus looking thing, or some chain of connectivity where for a web app routes each correspond to a change of functionality for a disorganized repository or for something that's a mess. What you'll instead see is code being imported from all over the place. You'll see no meaningful correspondence between the folder and file structure and the overall app structure. You'll see very large files. And that there's no coherence to it. Is it a server or CLI? The CLI with a single entry point and then some fan out for each of the arguments. Is it a server has a single entry point to run and then has all these different API endpoints that then go and feed the client? It's just a client application where you've got a bunch of uh fetch or some other sort of network call. Maybe it goes through Axios or some helper library, or there's some uh in repo helper that is where all the fetch calls go, essentially. Maybe it's using WebSockets or some other communication technology. Um even just at a glance there, you can basically go look at the kind of the edges, not the edges and uh nodes and edges, but like the terminal of the boundary, and get some feel for what is at the boundary to the network call, or is it a rendering TXX component, or is it a server endpoint, that kind of thing. I think related is there's a number of popular full stack frameworks, there's a number of popular front-end technologies, React View, Sol.js, felt, etc. I think you can very quickly detect which of those a given application is, and then maybe do something useful with that information. There's nothing else, it's the sort of thing that you could show the user. But I think all of these are the kind of immediate first goal is there is some visual which can show the totality of the repo, even if the repo is a million lines of code. Because even a million lines of code has some overall organization to it. There's a server, there's a client, there's microservice A, B, C, all the way out through X, Y, Z. But even the microservices might have some organization to them. These are the networking microservices, these are the image processing microservices. And so there is always some hierarchical roll up whereby you can show the relationship between the rolled up bits. And then you can allow the user to either dig into them. So uh let me go zoom into that region and just see what's going on. Let's sort of zoom in or isolation is to say, let me go treat this subsystem as an isolated thing. Let's treat that edge as crossing the boundary as just that edges crossing the boundary. We agree that those are going out or coming in. But we're going to look at the stuff in the middle, but those is the ins or the outs. Another way of doing it, or instead of isolating, you instead expand within the hierarchy. Maybe it's got 10 or 15 children in it. Maybe the rest of the diagram has 30 or 40 items in it. So you expand the one into the 10 or 15. Now you've got around 50 items on the screen. And those 50 items are going to be slightly more cluttered than before. But you've intentionally expanded a piece of it, and so your focus is there. Maybe some of that stuff that's cluttered but less useful or not related to the thing you just expanded. Maybe it's sort of fades to the periphery. Maybe it actually disappears. Maybe it just has a lower opacity or get smaller. Or maybe some of those unrelated things get rolled up into a node that says not related to expanded regions. Where you can read the groups. And so depending on the scale of the analysis, you can always find 30 or 50 things that make sense to be looked at together at a point in time. What are some other things to show for a repo-wide analysis? I think the most interesting ones are from the perspective of the user. So in a web application, the user is in a browser. They are click click clicking or interacting with some route. First thing that happens is they have to load the page, so they hit the route on a clean refresh, first time visit. What happens? The server is forced to load that route. So there's some route entry point. That's going to traverse through some code. It will eventually lead with something being given back to the client. Typically, that something these days is a server-side-rendered TFX thing. It's being filled or hydrated with code and data. That code and data is being fed because the component tree is asking for code and data. And so now that single request is causing a number of other systems to be called or invoked or their data loaded. Let's say, for example, we're looking at a product page, and in order to get that information, we have to go to the database with a product ID. The database comes back with a nice big fat chunk of data, which includes the name, the subtitle, the price, variations, etc. Maybe that data comes back and it kicks off a handful of other network requests to get some additional information. Turns out that product is on sale. Maybe that product can only be shipped to certain countries. Some feel when you're looking at this overall repo view. You'd like to get some feel of what are the discrete routes that exist. So on my blog. There's about five top level brows. There's the index page, there's the blog page, this product page, the experience page, and the about page. The blog page is really split in two because you've got the blog index page and then the blog details page. Projects and experience are all the same. Index and about are flat. And so what is that? Eight total routes on that site. Which to say there's eight unique ways that you can come in. And from those eight, we're going to go invoke a bunch of different systems. Now, nearly all those, I think, not several of those, are going to go through a MDX pipeline. So the actual thing that's rendered is a function of the markdown content. That's kind of got this whole dynamic mess of stuff. And so that's a little bit of indirection that might be worth knowing. Kind of messy. It's actually doing some file system discovery to figure out what's available. But ultimately, all of that has some amount of configuration or something that links strings to components, and by some scheme or another, the components are rendered when they're needed. And so an overall repo familiarization screen would convey that those routes exist. It would convey some of them with these markdown MDX renders. It should convey that the MDX has been configured with the suite of components that could render for each of those components and might convey. That one's just a demo on Sol J app. That one's just a table renderer, but the table is uh interactive data table that's a little bit nicer than normal. But it's still just a table, and unless you're interested in that code specifically, you could just treat it as it renders a table. Maybe some of those components actually fire off new network requests or trigger additional server functionality or invoke an image processing library for some reason. And I think all that would be the sort of information you'd expect to see in one of these kind of overall repo viewers. I guess I'm calling it a repo viewer, but really all of that is hinting at that's an application viewer. Like the goal there is to understand what is this application doing, what's its purpose? What are its inputs and outputs from the perspective of its purpose? Well, what's its purpose? It's a blog, it exists to not a blog, it's a portfolio page that has a blog. It exists to convey information. Exists to hold the blog, it makes us to hold this project information. It exists to provide the necessary functionality so that you can have interactive blog posts where the interactivity is arbitrary components, authored only for the purpose of demonstrating them. Interactive canvas explorer, I guess. And it creates a whole interactive canvas thing just to explain. So I think if you're trying to make sense of that repo, the application level, you would somehow see the routes, their parameters. On one side, you would see the data loading that takes place. You would then see the hierarchy of components that are rendered with that data. If there's a bunch of conditional rendering, you would somehow get a sense of the major forks and conditions somehow inside of the hierarchy. And I attempted to do this before. In the previous version, I rendered files as a tree map, which is to say I rendered rectangles that were deeply nested, rectangles inside rectangles inside rectangles, where the rectangles for size proportional to some measure, typically lines of code. In other cases it was complexity. But their size, if a scope declares stuff inside of it, it would grow with the size of the stuff that was inside of it. If there was a bunch of conditional stuff, either nested TSX or ternaries or optional chaining, take your pick. Those were all rendered as additional scopes inside the parent, so more boxes. And so it gave some sense that the complexity was inside there because you had all these nested children. Since the organizing unit there was the file, as long as the complexity was in a component in a different file, you would basically kind of hide it over there. Not hide it. If you got 30 boxes, that's just a question of which container is holding them. So the file called index or folder called chart biz. So the tree view was good from that point of view, or the tree map, in that it did not really let you get away from seeing the complexity of the program in total and seeing the size of the thing because everything was always scaled by some constant, some common parameter, like lines of code. But that again might be a reasonable practice. And so the tree map kind of had this implied assumption that things which called or were needed for each other were code located. And if you wanted to roughly understand the size or complexity of a feature, you put all the related stuff into a common parent folder, common parent file, and then you could go measure the complexity of a subsystem by picking the appropriate parent node and comparing its size to the size of everything else. That's how you could convey that one area had substantially more code and probably complexity than another area. But that uh was not helpful at showing how data actually flows through an application and was not helpful at showing how unnecessary complexity might have been added to the application because of gross or sloppy data flows. You can imagine if you had to, that you could declare a single master top level type called product. Product could convey title, subtitle, region availability, price, pricing schemes, where this top-level object has all sorts of possible complexity in the data types underneath it. And if you were in C sharp.net land, you probably actually just declare a class called product. You would have all these objects or properties hanging off of it. Those might then refer to their own complicated classes. Pricing schemes might be some very nasty thing that can tell you might be some very nasty thing that can tell you the price of the object on a given date, and it needs access to a user ID. And the user ID is gonna go look up some other stuff and some other stuff. Maybe even for the pricing scheme, you just have a pricing scheme ID, which is for a given region because you're just editing a given region. And so then the data object that you're working on is some subset of a class that normally is driven by product, but on the page you're looking at, you're just calling an API endpoint that returns five or six pieces of data so you can see them, so you can edit them, and it might be lost that you're actually working on this pricing scheme data type. Why does all that matter? Well, because then you go in and somebody reconfigures the pricing scheme data type, because they've added some new condition that in addition to the user and their region. So in order to make that happen, they've added a full tagging system to the pricing regime, and they want people that are managing these products to be able to go add in pricing rules that are tag based. That's now on some subset of your pricing regime admin page ID if you go wire in this tag functionality. That derives from maybe product, if not product, at least this pricing regime thing. Because then if you can go find everything that's related to a pricing pricing regime, you can quickly make sense of did we correctly wire up this tag-based stuff. And at least if you got it wrong, if you're in.NETLAND you'd get compile errors. But the problem is if you're declaring new one off types, new one-off data transfer objects or DTOs, you're now in the world of having to go mirroring the types. If you're doing things perfectly, you should ideally just bring the full type along for the ride. If bringing the full type along is super messy because the front end doesn't actually need it everywhere, you should at least go pick from the base type the fields you want, that we're kind of maintaining this correspondence back. And so if you're trying to go through an auto code base that's doing all this stuff, it'd be really nice to understand when you're accessing things that look like a given type, but are actually being declared as a new type that is technically not linked to the original type, which is to say adding something to the base type like these tags is not going to be reflected in the new derived type. And so thinking about the what are the types that flow through the system, really it's what are the data types or the canonical business data types that are flowing through the system. Because those things should realistically be maintained all the way up until the point that you render something. And maybe because it eases front end development to be able to create a component that's just derived from something. That's fine. But you still want to have the type system be fully linked up into end so that anytime you're doing one of these derived or subtypes it's being done with a pick or something that somehow is actually linked to the base data type. And I think this is one of the most disorienting things that the coding agents are getting wrong, which is that they're declaring all sorts of new data types here, there and everywhere and they are just small variations in something that already existed. But they're giving it a new name and in a lot of cases they're just recreating the pipe. In some cases they're linking it back but they're changing the name enough that when you go to make sense of these you can't and most importantly but important now if you flow through your system you basically have a new name at each spot each transformation comes with an extra name. Alright, so what's the problem with having a bunch of extra names? Well, these extra names tend to uh just pollute. They tend to lead to more extra names. They tend to lead to real lack of clarity around what is being edited, what is being viewed. And then in front of the application where you're getting some amount of data in, and then you need to act on it to go update. You now have this problem that the data type that you're eventually going to go update in the database is some base type, way up I, but all of the viewing types are six steps removed from the base type. So you have to do this massive reconciliation, going up the tree through all these subtypes to then actually land an edit on the base type. And I think that in addition to problem, just the complexity of these types and names all over the place. I think that is one of the true fundamental problems with a lot of the agent encoding the types. At the very top, the canonical database types are being transformed, reshaped, shuffled around, reworked, various sorts of fallbacks and defaults are being added in at various points. And then three, four, five, six, ten steps removed from the database. You then have to put it all back together again so you can go update the database based off something that's happening way deep down. Or you're trying to audit debug, troubleshoot an error, and there's just this huge mess of complexity, and very often the errors are resolved by just adding a little bit more complexity, and that really falls apart because the agents are very prone to take the specifics of a single scenario. Oh, I've got this airing case. Oh this data type was subtly changed. But if we make this one correction in this one scenario, we can correct it. And now of a sudden, you've got these Christmas trees of branches because you started adding in one-off fixes due to some missing statement, some missing observation or correspondence way off the tree. So the next error that comes through leads to another branch with a fallback with another, just the thing that has to be maintained. So I think if you could just visualize that all of these data type transformations are taking place as you go from the database down to the renderer, you could just get some sense of ah, this is where we went wrong. We extracted that thing out from its parent. We then allowed it to get subtly transformed a couple times, and then we attempted to reconcile and edit back into the original spot. But we'd removed enough of the linking information. And we had to go do some magic, put it back together. Alright, so if all that is like true, that's a problem. What would I just expect to see in a reprofife visualizer? I would expect to see that some data coming from the database has a given shape. And in order for a specific route to render, that data has to come through, and some of that data is being used for lookups and other sorts of uh transformations. And then in other places, some of that data is being manipulated for some client-side logic. So for example, the note-taking website has a time block explorer page. Time blocks are these objects that have a start time and an end time, the title, it can be linked to a task. And so as you're choosing how to manipulate that thing, you're dragging it around, you're resizing it on screen. All of this gives you some. Some amount of actual state in the database or some amount of as rendered on the screen. You've got all these scales and things that are taking the dates and then running them through. Figure out how to position them, how to actually render them in the viewport. And so there's this big transformation going on from the raw data dates into the X, Y positions for the viewport. There's some additional logic going on. And the front end to the techniques that are overlapping. So then when you start dragging, something might be a third the width of the full column because it's overlapping. As it drags around, you need the width to update. And so the width is some function of column size, which is a function of the viewport size. The width is also a function of the drag state. Because if you drag it around, the width might change. And then when it eventually lands, also the X and really the Y position, the vertical position are changing. I guess the X is changing too because the width is changing. And so we might, when it gets bigger, the X needs to change. So previously the Y position was being driven by the date and time. It's still being driven by the date and time, but now it's being driven by the cursors, date and time. And so we've got this uh very large state problem on the client, where the starting point is certainly the database. As all these things drag around, it fires off an update. You then end up with uh optimistic update. So the client state is sitting there rendering the new one, so there's a bump. Then the server has to come through with the update, you've got to reconcile them all. And then all of that has to float into the component to ultimately determine the X and Y position of this block. And so if I just want to see all that complexity, somewhere at the very end of the line, there's a well, there's at least one div, or a DOM element, there's probably more than one. It most likely has a style attribute. It's got an X and a Y on it, maybe a top and a left. Maybe it has a style attribute with the transform with a translate in there. This far X, this bar Y. Maybe there is VG in the rectangles. I know if they are. I think DOM elements. Not SVG. But even this just tends to add it, which is you've got this box that will eventually render on screen. For the position, you'd like to be able to go investigate that. And so to make sense of that problem, now you're in the case where you're looking at the sync, the SINK sync, which is the terminal node. It might be you've actually got multiple terminal nodes, because maybe you render one thing that's at rest. Maybe you render a different thing that's moving around. Or maybe you've got a single time block that gets rendered in both cases. Maybe it'd be the style attribute that has a conditional in it based off of is dragging. If you just had a guess, we've got a item that corresponds to the database entry. It has inside of it enough state logic that's being driven by the source data, the drag data, and then it resolves its position. It assumes that's being rendered inside some time block grid. But even that I guess is up to grabs because you could render uh a column for each thing in the grid. And so maybe you've got the client doing some pre-grouping of columns. I think I actually made that mistake. Have the client group by column, go render the columns individually, and then in the scenario where somebody drags it from one column to the next, you have to keep track of all that, send the thing over. It's a terrible mess. Sort of hints at why the first option is better. You just want the container to know that it can render time blocks. You want them to be positioned with their X and Y, able to just be interactive. That way you've got a single surface that contains them all, makes it way easier to do animations and the rest of it. But if you've actually made this mistake of grouping things too early, it'd be really nice to be able to see that. Or even just uh you maybe don't actually group them early, but you render the columns in some way. Because the other thing can happen in that code. Like you've got this grid, you need column labels, you need kind of this sense of column border, so you're kind of rendering these columns. But you've also got rows that need to render, because the rows are indicating the blocks of time. You've got cells that need to render. And just talking through this, what you'd expect to see is like a single kind of grid surface. You've got the column headers, you've got grid lines, maybe, you've got the rows, you render those in the correct order, so that's the Z index is right and things layer correctly. You render the cells at the very end on top of it all. And that cell should just be a flat list with their X and Y positions, determined by a whole stack of statefulness, also being driven by this idea of a viewport scale, viewport mapping, mapping dates to X, Y positions. And so if I was trying to audit that component, I'd want to be able to go see the cells trace through the X and Y position. I'd like to somehow be able to see if these forks are going on. Are we splitting based off of the is dragging? If so, how's that being done? And then if we're taking the same data object and rendering it three, four, five different ways, conditionally, based off of something like an is dragging state. Really, how does this render? What are the ways that these boxes end up on screen? What are the ways that the drag state affects all this, and maybe some other stuff? Uh what are the parent contexts that are feeding into all these components? So I guess that's all the information that I would want to be available. Some of that is certainly not going to show up in the single repo viewer because we've dug way into the specifics. Alright, we'll stop there.