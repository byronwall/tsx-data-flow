Alright, here we go. This will be a note about new or different ways of doing static analysis, just views of a code base that might be helpful. In particular, the goal is to think about all the intermediate transformations from one type to another to another to another and put a number on those and then go reduce them or remove them. At the most fundamental level, you've got whatever's in the database, and then you've got layers of transformations from there as you work your way into the application, work your way through all the client state, work your way into all of the fancy interactive components, and then work your way back through all the mutations and updates and things that make changes to the database to close the loop and go again. For example, drag and drop. You might be rendering cards on a canvas. You might have some parent child relationship. And so you pick up a card to start dragging it. And now you need to track its position on the canvas. Maybe it's just an absolute position, temporary override kind of thing. But you're tracking the position, you're tracking maybe the card index of the ID or some object or something. That way when you do the drop, you have nice smooth fluid motion through the drag into the drop. The drop can correctly detect the new parent component. You get nice visuals through out it all just works. There's some actual storage being done there. You know that nodes are arranged, children belonging to a parent. Stuff disappears. But then in order to support the drag and drop, you need to have this position abstract on. Ranged on the canvas. Maybe here position is not a deterministic thing, but instead it's a user is moving things around because they want them to have a certain position. And so now you've got the original node, you've got this position that you're tracking. Maybe uh nodes can belong to an arbitrary number of these boards, and so you need to separately store the node ID, probably the position, and then you're off to the races. But then maybe in that same display, you've got a separate layer that lets you do some color legend stuff. So next to all of the nodes that are in the shopping category, you color those blue, and then the other category you color those red. All that needs to get saved in the database, and all that needs to come together. And so that right there, you have really three different pieces of data. You got like a node ID, you've got the position, you've got a category. Category is really derived from the node. The category color is a property of the board. And depending on how all of that is actually wired out, you may very well end up with two or three extra data structures along the way. Because what happens if you don't have your color map? Well, you've got to get some default colors out there. Maybe you get some global color map. And all of a sudden you gotta pull a mess because there's three, four, five different ways for a piece of data to flow into a given spot. And you've got three, four, five different ways for information from a node to be consumed in other places. And it's a legitimate question as to whether you just pass the node around everywhere or the node ID. But these are the sorts of things that start to add data structures. Another example. It can do either. No, it's just being given HTML. And so now you've got this problem where a property of an object is flowing into a component. And you've kind of lost the lost the boundary there. You know that a string of HTML is going in, string of HTML is coming out. But if you needed to do some validation or something, you've now got this extra work where the component is not working on your object, but you see the string of HTML. So you got this boundary where you're probably sending data back and forth, back and forth. Because maybe inside the HTML, for example, inside the HTML, you may have references to other documents. All of that's good and fine. But when you go to save it, it all gets collapsed back to this HTML. So you've got to be certain that you can basically render it, save it, render it, save it. I'm not losing something. And so what's the point there? The point there is that sometimes you end up taking these known data structures and just stuffing them into something like an HTML string. And then you need to know that those might be able to come out of there. Especially if you're going to store additional data inside the HTML tag. Because maybe what's something you can store. You can store a diagram. You can store a small little picture. You can store the scaling on an image. That's an actual thing I had to do. An image and a diagram, an image in a document editor. Sometimes you want the image to be left, right, or centered. Sometimes you want the image to be full width. Sometimes you want it to be 100% of its own size. Sometimes you want it to be a number in between. Or 50% of the really it's the container document. And so now we've got this image, probably an image tag. We'll put attributes on it. And now we need to know that that's that those attributes may or may not be present. And if they're present, we need to do something with them. Manipulating them, need to be aware of the fact that the client has more up-to-date information than the server around what size should be there. And so where do you store that state in the component of the local override, in the HTML string, you're just reparsing it constantly. Both. And then if you're trying to go and do static analysis and all this, will you actually see that all this is happening? Probably not. Because you've got this kind of boundary that the component's creating where it knows that it's capable of going into HTML and reading all this stuff out. And your database is storing the string. So hopefully it's storing it well enough. But if you're going to persist all that noise as HTML, you have this fundamental problem that you're gonna lose visibility into the types and the references, unless you're gonna do something special or different. Well, what could you do? You could have a separate database table that stores all the links as a HTML comes through, we do a post-processing, we extract all the links, we put them in a dedicated table, and so now we know that this document links to that document, even though ground truth is just a string of HTML, and we're hanging on by our fingertips with this post-processed wing finding thing, just so that it's moderately efficient to do a reverse lookup on a link. So how'd static analysis help you there? Well, one possibility is that you might just detect all of these crazy parsing operations. So inside the document editing component, you'd surely see this sort of plugin system with its wacky string to component type logic. All of this parsing, you'd presumably see some outer layer that's handing. Or has a callback. Maybe those components are accessing some global state. And those guys are somehow or another being given the information about what they should go show. And so with the right visual, you should be able to see all that. And maybe you would see some sort of hotspot of calls going through some global data channel. And maybe also that all of that is as good as it can be, given that you're doing this store it all on HTML approach. But then you might say, okay, how would it look different? If instead of storing it as HTML, you're storing it as a typed array of blocks. Well now you've removed the parsing step. And so I would expect if you get this array of type blocks, blocks that have a type, you know, some parameter called type. That block is a document link reference. And then you would have some dictionary or lookup that says the document link reference. That thing is down the specific type of data coming in. So we know that if it is a block type, it has an ID. We can use that ID into database calls. We go get the fields we want to render so that on cover that shows up correctly. All that seems pretty reasonable. And so if you're in that world, what does the database look like? You could very well just be storing these blocks in the database. They have a type. The type jumps you over to another table or jumps you into some JSON blob. And inside there, you're storing a link for the metadata or whatever. Something so then what would be the goal on a system like this. You would want to know if you're trying to make a change in here. How do I add a new renderer? How do I wear that? How do I add one and get it basically for free? That all the data just flows through correctly. How do I have high confidence that I'm saving something? Not accidentally blowing away an attribute that's critical. This is where all the type of SP can help you a ton. Because now instead of having to parse HTML and verify that it's correct, you can instead just persist from JSON block, have a validator check it. You could say with certainty block we're putting under the database is that play the correct type. We know it's right. That all feels pretty good. Could you get that with HTML? Yes, you could if you're parsing that. But now you basically have to uh use HTML validation step before you actually save it to the database. Inside there is where you would go parse everything and basically you'll build these types from the HTML. It certainly could be done. If you're doing static analysis, is where are the places where you go from having an object with a known named type, maybe even a type that's in a pool of these are my blessed data structures. When does that thing touch a transformation where you're only keeping some of the properties? A well-known object, like a thing called a concept. And it has a name, has a backing store. It's got five or six or ten different properties. And then because you want to make it so that the title can be edited. You take the title and pass it into a component called edit in place. And your edit in place only knows about the string that it's been given. That's no good. And so now we've got this spot here where this object is being operated on twice once we get the title. And again, to do the callback. You're carrying your object all the way to the finish line. And you're good to go. Does that look like less well? Well, now you're carrying your object into a component that's using a field of the object. And then somehow doing some logic on that to either get back to the object via a lookup, or it's using that field to go get to some other object. So instead of saying like uh concept.parent, you instead pull in the parent ID, and you go do a lookup somewhere else. You say get concept from this parent ID. So we've kind of wandered the relationship through the scalar or the primitive and think of this as like uh types having like a size or something. We've gone from an object, we've narrowed to a single primitive. All of the possible fields we had access to have been reduced to one. We just have this ID, but then we take that ID and we fan back out because on the flip side of it, using that ID and we're using it to get a big object again. And so if you were to draw these visually from left to right where you've got input and output, you're sort of watching the types as they transform. These things would look almost like uh I'm coming in with five lines for each of the fields that I have access to. And boom, right there, you dropped a dumb line, it's just the same that might be. Got one line then expands back out. So I wonder if that would be an interesting visual of this. I'm going to think about it as nodes and edges where you're conveying height with the style or rendering of the edge itself. Where in the simplest case we draw a line with each field that's inside there. Some of the fields will have types which also exist in your system. So maybe you give those a color or a style sort of like a subway diagram so certain styles for certain types of roads. Well that's a highway actually we've got some composite data structure that's carrying around screen around six concepts. Boom right there. It took concept number two grabbed its ID plunged that off to somewhere else. Alright so the visual then is or conveying the types edges convey the data available on a field. And so the hope is that you could use this to detect the places where you're going up and down and getting a scalar and then banding back out to an object. And then in some cases, the question is, are you going in and out to an object that's of the same height, so you just need to pass it through. In other cases, where you're building these large composite objects, and then just passing them around. In other cases, you probably pass around lists of objects. So what would that look like? Anything specific? I don't know. Imagine you pass around lists of objects, and then maybe you're only keeping five of them. And so we're going from a list to a single item. Sort of way to convey that. It's like we've reduced from many to one. Maybe. And so I think that data flow analysis is very helpful. And it's like this is an additional layer on top of the data flow analysis. And maybe I start at a sync. And I say, where did this thing get its data from? So this is data flow where we're asking. What's the path that gives this thing its data? And then along that path, what are the type transformations being done? Or is it simply plucking and renaming a data flow program called that an alias or a representation only change? And then I think if you're thinking about it like that, there are definitely places in the application where you say from here I'm safe. So if you've done a lookup with an ID from there, you can definitely trust that the data is correct. But it still might be a very contorted path to get there. Because maybe that ID came from another lookup, came from another lookup. Maybe thousands of syncs with tens of thousands of edges and you can't really render all of that you kind of could you can maybe render it if you're starting to group stuff by file or feature or area maybe use color to convey things that uh touch a certain type or are poisoned by a certain type um we start there it's like we're just gonna go render that graph we've got it and then we're probably gonna be very interested in either a component which is to say a group or a container near the end or maybe a component or a container near the middle because it's one of these areas where a ton of math and logic is being done before we hand off to a specialized component. So for the DOM capture tool maybe the capture viewer is one of these it's getting a trace which is this big data structure but then it's having to decompose that trace into all of the selectable things or it's having to run through there and do the typography analysis. And so as it's doing that it's spitting out tons and tons of intermediate data structures that are derived from the database and so then, depending on what's visible, you could very well be rendering a list of things that this result is some really gnarly math. So there's probably a way to visually show that. Probably in the beginning, can't worry about the list versus single. Let's think about how to render that differently. Because it is an interesting question. It's one thing to say the data type is a list. It's another to really get to it that there could be a giant pile of stuff there that you need to go deal with. It's like, what are you going to do with the list? Um at some point, you're either going to process each item, so you're gonna map or for somehow loop over them. Now you're able to go from a list down to single items again. Or you're going to index into the list. So you're gonna go select, and again that's gonna get you to go from many to one. Or you're gonna do some sort of aggregation where you're iterating over the list and keeping some single value. So it's basically a selection from a different way, a selection after a calculation, where you might be legitimately transforming the data type. You've got a list of purchases of the sale price, you're computing total revenue. So then when you go and render total revenue, it is just a number. But the number that was derived from possibly a long list. Or worse, you go from one to many. I go load the children nodes. Um what else? I've got a search string and I go get a list of matches. These are many many items that follow from one. I bet there's something interesting about seeing those as well. Alright, let's leave it there for now.