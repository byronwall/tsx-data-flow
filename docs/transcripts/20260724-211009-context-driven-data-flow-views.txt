Alright, here we go. Alright, this one's gonna be about the TFX data flow process specifically. It's around this idea of what is the best way to get an overview of data flows when you've got a set of components that are all nested and they are all consuming the same context and they are all more or less pulling the same object out of context. So for example, I've got the pluck UI program. Got the idea of a page capture and on things that touch the capture object. Nearly every component in the tree has the same context. Something like that. And they are all pulling from that, the object called page. And then they are pulling from that. Who knows what, but presumably all the fields that they're actually using. And so we've gone through this process of identifying that the capture data source is being accessed, that's being put into the context. From the context it is flowing through. But we've got this new problem that on a component tree like this, there's no real discrimination going on because every single component has the same context, is touching the same object. And so when I go to filter by source data flow, it basically just touches everything. Everything lights up and there's no filtering. And so okay, what is what should we do about that? First thing to do. But it seems that's probably the first assumption that's bad, because in this case everything is pulling off the same object. And it's not really telling you much about how the data is flowing. It's kind of obvious that they all need that object. I assume what's going on is that the page object is a type with many fields and other things attached to it, and that the components are doing different stuff if you go and look at it from that field level point of view. Going into a context and coming out. We allow for a little more granularity so that we can detect that the page object has fields, and then we go trace those fields instead of the top level page. And I think that would work fine. Which is if you're gonna go differentiating fields on the data source, what's the limiting principle? Do you allow the user to trace every arbitrary field all the way through the deep nesting, maybe? I mean, I think honestly, the answer is yes if that's what they want to do. I think the problem, well yeah, and it's like yes if that's what they want to do. I think the original assumption was correct though, that if you're coming onto a random page and look at this route, I see three or four data sources in a lot of applications. You actually get a pretty good bit of split just by looking at who's using which data source. Now we've stumbled into a set of components where that's just not useful, or it's useful in that it reveals everything uses it. So then you want to have some more machinery available. In this case, that means hey, literally every single page is using every single component is using this object called page. We need to do that differently. We need to allow the user to see the fields of page and go trace those instead. Yeah, I think that's right. So now at the data source, and I'm already doing this. I showed the type. Hey, this is a type capture, whatever. I can easily show the subtype. Or I could allow the user to uh filter by this type, see the fields on it, uh filter by the flow on that field instead, and now you'd likely start to see certain things being used in some places and not in others. Then I guess this kind of hints at okay, I assume we can do all this stuff, but like to what end? What's the actual goal on all this? And maybe this is where it's worth like stepping back and seeing what is the goal in all this. The original premise was that if you can show up to a graph of a component tree and see the data flowing through it, you should be able to make sense of which things are uh being used correctly, incorrectly. Is that the idea? Um you can get a feel for which things are derived from one source for versus another. I think there's some truth to that. You might be able to get a feel for how to split or refactor. Not sure about that. I'll take another step back. I mean I mean part of this was my concern is that the data's coming in and that it's being manipulated, transformed, and that we've got a bunch of extra steps in there that are unnecessary. And I guess maybe this is just a pinch of evidence that maybe that's not what's going on because you've got this context and everybody's accessing the same variable. At least that's what it sure looks like. Um I mean the other possibility is this just reveals hey this variable is used everywhere. It's important. That's not surprising. It still leaves it open the question for all the paths that the object is following. What is happening when it touches the variable on its way to being rendered because it using the context like this means that we've very likely shortened the path. It's like yes, the data source is flowing through all of these, but also it's basically just jumping through all of them. It's like I'm actually okay if every page is every component is using the context, grabbing the object, rendering the piece of it they need. Um that's probably okay, unless they're all doing the same three, four, five transformations, and those should realistically be done once up above, and then that pass through. Okay, so that's that's one possible negative to all this is that by having everybody grab the raw context and grab the raw data, they're very likely re-implementing the same transformations if they're not being done and then put into a common place. What else could be happening? Another possibility is that we've got these contacts, and some consumers are using them. Uh they're just going straight through, no transforms, nothing funky, just getting the data and using it, life is good. But then maybe a couple of other components are transforming and then passing that value down into a component tree, and instead of using context, now we've got a prompt chain that's flowing. And then I think the question really does become, was that good or bad? It's probably good if the components that are receiving the values are general purpose, UI, render rehearsed. Because you really don't want those things to have random contexts inside of them. That makes no sense. Well that's not how it works. And so then there's probably some sort of uh convention lurking, which is that components in a certain area should not have contexts from another area. And very likely maybe they don't have contexts at all. But it's reasonable to say they definitely don't just directly derive from source data. There needs to be something in the middle that's actually blocking the value and passing it through. So it's almost like the thing that is a problem, possibly, and the sort of feature data flow side of things, is then exactly what we want when we're getting out to the leaf and we're using a general purpose component. Somewhere in there you have to have a step. And so I guess that's a piece of what's going on here. It's just like observing that chain of transformations. And I mean, just looking at the uh path counts. There are a huge number of chains of data flows going through the application. And so it's not reasonable to go look at all of them. But then what is the actual goal here? Um I mean, maybe a piece of the goal is just to say, man, this route sure has a lot of complexity. Do we really need to be doing all of this? It's possible. Another option is that we've got a lot of common helpers, and they have a lot of complexity underneath them. But we've got a pretty clean handoff from one to the next. Safe or not interesting, which then allows you to focus on the other components which are interesting. Alright, let's assume that's true what that looks like. Well, in these uh capture pages, I've got some inspectors. The inspectors have these typography sample renders. So they're being given something related to typography, and then they're rendering out these samples of how the typography behaves. If you give it the data in the correct shape, it's good to go. Which then means all of its subtree is not interesting. It's almost like it's a boundary. If you can get the data to the spot in the right shape, it's fine. And then you go looking upstream to see what is the path to get the data in there. Oh, we've actually got a really crazy 3456 stack of memo calls, and they are building this new data structure. And then you'd look at that and say, well, take that 3456 stack of things, refactor it out, and so now from a call graph point of view, I just have one hop in, one hop out. Now technically inside there, I still have all the same data flows going on. But if you're just looking at it as like inputs and outputs, maybe we declare that functions are sort of like a boundary. Well, then that's much cleaner because I've got a single function that's taking two or three inputs, returning one output, it lives over there in its own file. And now we're passing some data into that. It's going through the rendering, this typography sample thing, and in theory, all that's cleaner. Because I think all that's possibly true, but it really hinges on the assumption that I'm doing a bunch of inline transforms and object building in my render function. So that's what I'm worried about. Shouldn't I just go tally that stuff up? Like if what I'm trying to determine is nastiness inside components related to complicated data flows, then why go to the effort of detecting the sources and tracing them all the way through every component from source all the way to sync, just to then say, hey, inside that component there, we're detecting complexity on this data flow. Like you could just go look at the component and detect the complexity. You could go determine that it's got four, five, ten helpers in line. There's four, five, ten helpers fan out uh 10, 20, 30 props across a range of child components. And what do we do better to just go search for those sorts of problems? Yeah, yeah, pull that through. Okay, so it's like uh what are we really doing here? Searching for problems or investigating data flow. Because if we're investigating data flow, on the one hand, there's just this like big picture. Let's go see how the data flows. But then on this components here, all tracing the data flows isn't that interesting because the data flows into every component exactly the way you'd expect in this tree. Because, you know, quite intentionally, I went and added the context that we've more properly. And so then lo and behold, we're using context everywhere, and so we can see that. Uh now we've got this new question of to the extent we have this context, what is going on inside the component as the variables coming out of context are going somewhere else. Yeah. And so, okay, so there's there's like a legit line of analysis you could go run. Detect that we have a context, detect that we're doing access on the context, detect that we're doing transformations or some other calls, and that the context is ultimately getting routed into a range of places. And so maybe that is the view of interest. What is the data flow from this context going to other places? And that is a little bit of the spirit of this view. I just observed early on that if the context is being consumed in 30 places. Don't go draw all the arrows. Just say, hey, we are looking at the first pop from the context. Where does it go next? And so you might see that we grabbed a page variable, we grab the borders variable, we grab whichever variables. Okay, so now what are like we look at the type. We've got the context, we've got its type, and then it's like, what is the immediate first thing that happens on that object? Do we grab a field? Okay, maybe. What do we do with the field? We pass it into a function, okay? Maybe. If we're passing it into a function, how many times we pass it into the same function? And then you might detect, oh, we're actually grabbing the page variable and passing it into this, extract borders from page, we do that 30 times. And so it wouldn't we do better to update the context so that that my call is done once, and then everybody can just go grab the borders variable. And then you'd play it out again. Okay, now what happens to the borders variable. So I think that is actually probably an interesting view, which is for some of these objects that are accessed repeatedly and accessed repeatedly and across the component tree. What is the data flow path leaving from there? And so we call the context. Go render all of those. And then while you're rendering it, just give some sense of like count and uh like where it's happening. And we're just trying to detect common patterns that can be streamlined, which is to say brought into a common helper. That way we're not repeating logic in three, four, five, ten places in order to extract the same variable or produce the same intermediate result. And then from there we'll eventually see oh, this uh ch this sequence ends in a text field in a box in uh this component here. I guess at that point you could uh that's probably good enough. Because then if you're wanting to go investigate that a little bit more, you might say, well, does that component is it only receiving that prop from this chain? Which for context means you'd honestly be better to move the context access into the child and have whatever data transforms are going on take place inside there one time rather than doing all these steps outside and then passing it through three, four, five times. But you kind of need this view of from contacts on out. Um I guess it's similar but a little bit different because at the moment the view is from a data source all the way to the sinks, and then we happen to be tracing through the contacts just as a way of indicating this data source touches that component. And that is I I think that is a very useful view because you can start to see oh, we're actually accessing the same data from two different entry points, and there's really no reason to be doing that. Like we've got two highly correlated, highly overlapping Prisma getters, and we do better just to reduce that to one, because in this component tree, obviously the one is already available. If we were using use query or something, it would be cache, and we basically get it for free, but instead, we're taking a second run up, getting possibly the same or different data. I guess that's another possibility because, like, I'm sort of assuming that the uh method call function call is with the same parameters to get the same result. But it is entirely possible that they're being called on different chunks of data. And so this gets a little bit more into the world of like you'd actually need to trace the thing at runtime to see how things are being called. It may not be enough to see that we're accessing the same data types because it could be different instances of the same data type. And obviously, that depends a ton on the data type. If the data type is uh, you know, employee. Well, it could be a whole slew of reasons for grabbing employee, and just because we're grabbing employee in one place, I mean we're grabbing employee in another place to get the same object. One could be the manager, the other could be the employee. Okay, so this is one possibility. It's like, oh, what I'm trying to actually understand is our context usage sensible. So let's get a dedicated view for understanding context usage. It's going to look similar because it's data source view, but ultimately we've got a different thing that's starting out on the far left. We've got a slightly different chain or sequence on the far right. I guess this kind of gets to the next thing, which is the view at the moment is very uh component-centric. That's fine. That was the goal. It's just to see how are the components calling each other. But another way of doing this is to like actually render the full data paths as chains. And so it's almost like I'm charting a trajectory of a data flow through lines of code. And then making sense of how things flow through, not at the component level, but at a more granular level. Well, this component is made up of these individual scopes. Here's how the uh edges and blocks put together when each node represents another scope. And this starts to recover the uh, honestly starts to recover almost the tree view, where okay, we'll just take that to its extreme. Let's show every scope as like a messy container. Um, that was not the most useful sort of thing. It's useful in detecting that you've got a lot of messy scopes, and so it's like why are you doing that? But it's less useful in the sense of uh it actually helps me understand how this beta is working its way through. Alright, but that does hit at kind of another angle here, which is I've got a component that's actually uh 400 lines of code. What all is going on in there? And is there a way to arrange it so that I'm locating the data only with the DOM elements that need it? Alright, yeah, so this is sort of in that idea of if I've got a large component that's doing five or six things, and imagine there's three, four, five, ten blocks at the top, you know, a big stack of memos and use effect or whatever, create effect. Um is it possible that I can find a way to split those out? If one of those memos just exists, you go render a list that's at the bottom of the page. Maybe I yank that list out, I yank the memo out. I give this new component a single prop, which is the incoming data. I go render it, and now my previously big component is a hundred lines shorter, and to the extent I'm thinking about these as almost like colorful blocks. Where does it end up? Um if I've got chunks of DOM that only depend on one or two data fields above. Is it possible to rearrange things? Refactor them so that stuff is just located where it needs to be for having these big jumbo components. The jungle components have this bad property that very often when you're changing or fixing one thing, you're accidentally breaking another thing because you got three or four concerns in there, and it's very difficult to separate them. Or it's very difficult to make a change in one area without having to impact something else. But then if that sort of thing is the problem, it basically hints at uh well the goal there is like this is a wholly different feature. It's almost like a component breakdown feature. Go show me the components that have the most stuff in them. Now I'm rendering some sort of a view edges and blocks for the component itself. And then maybe I am tracing data flows through there, or maybe I'm just doing this coloring exercise, and so it's helping me understand on these giant components what's going on. And there probably is a written report to be made. Hey, this component is got 15 blocks at the top. It's got 36 total nested scopes. You've got a couple clear seams in here where you could refactor this into three or four components instead of one big one. Once you go ahead and do the refactoring, then you can make sense of. Oh hey, that's actually using this context. And it's using it in the exact same way as this other component. Let's go refactor that. And so then the data sources that are worth tracking are the ones that are used less than that. Using only half of the components, well we'll check. I think if you're gonna do that you need to make it aware of it's like it it it needs to start being aware of hey this is a UI component don't trace data into it treat it as terminal, even though it's technically got children inside it there's probably some convention there if it's in the UI folder. Okay that's definitely just that based endpoint go arrange those accordingly if it is in the feature tree maybe there's certain features that are just tagged as such hey this is effectively UI but then maybe that's the hint that those should just be moved into UI. Hey these are common helpers we don't need to we don't need to trace into it I know I need to see some examples to see what that feels like and the end goal of all that is really to just reduce the total number of nodes on the screen when I'm looking at a route it's like I want to get a feel for the total float of stuff and seeing deep children this is not helpful. Maybe the other option here is just a limited depth uh show 10 hops by default if it's more than that you just gotta expand it. Is that really interesting? Maybe okay up there.