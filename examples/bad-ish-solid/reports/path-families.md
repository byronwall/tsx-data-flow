# Path Families

> Generated by **tsx-dataflow**, a static render-path data-flow analyzer for TypeScript/TSX (Solid/SolidStart-aware), from `examples/bad-ish-solid` on 2026-06-29. It parses the source and builds a graph from each value (props, signals, hooks, locals) through every transformation to the JSX **sink** that renders it, then ranks the heaviest render paths.
>
> Terminology: _source_/_sink_ name code positions in the static graph; counts, scores, reach, depth, and wrapper steps are analyzer-computed signals, not product accounts or runtime telemetry. A _wrapper step_ is a representation-only hop such as an alias, object pack, or spread; it can signal reviewability burden, but it should be weighed with burden score, defensive operation entries, reach, and pack/relay verdicts before choosing work.
>
> Groups render paths by structural _signature_ (a depth band plus the sequence of operation kinds) so recurring shapes surface. _Paths_/_Sinks_ count the family's members and _Max depth_ is the deepest path in it.

| Signature                                                    | Paths | Sinks | Max depth |
| ------------------------------------------------------------ | ----- | ----- | --------- |
| `shallow jsx-sink`                                           | 10    | 10    | 3         |
| `medium object-pack -> conditional -> jsx-sink`              | 1     | 1     | 5         |
| `deep call -> conditional -> jsx-sink`                       | 1     | 1     | 9         |
| `shallow conditional -> jsx-sink`                            | 3     | 3     | 3         |
| `medium call -> jsx-sink`                                    | 3     | 3     | 6         |
| `shallow call -> jsx-sink`                                   | 3     | 3     | 3         |
| `medium jsx-sink`                                            | 2     | 2     | 4         |
| `deep object-pack -> call -> impossible-defense -> jsx-sink` | 4     | 4     | 15        |

## Representative examples

### `deep object-pack -> call -> impossible-defense -> jsx-sink` — 4 sinks, max depth 15

Deepest member: `src/DashboardShell.tsx:111`

```
-> props  [source]
-> preferences  [property-read]
-> accentColor  [property-read]
-> «preferences.accentColor» ?? "#3f7f6f"  [fallback]
-> …ask, owner: user, swatch: «preferences.accentColor ?? "#3f7f6f"», }  [object-pack]
-> packed  [alias]
-> task  [property-read]
-> estimateHours  [property-read]
-> «packed.task.estimateHours» ?? 0  [fallback]
-> ${«packed.task.estimateHours ?? 0»}h  [template]
-> … "normal", estimateLabel: «${packed.task.estimateHours ?? 0}h», swatch: packed.swatch, }  [object-pack]
-> taskRowView  [call]
-> row  [alias]
-> ownerLabel  [property-read]
-> «row.ownerLabel» ?? "Unknown owner"  [fallback]
```

### `shallow jsx-sink` — 10 sinks, max depth 3

Deepest member: `src/DashboardShell.tsx:84`

```
-> props  [source]
-> tasks  [property-read]
-> task  [iteration]
```

### `medium call -> jsx-sink` — 3 sinks, max depth 6

Deepest member: `src/Chart.tsx:46`

```
-> props  [source]
-> tasks  [property-read]
-> groupBarSeries  [call]
-> barData  [call]
-> map  [call]
-> barSeries  [call]
```

### `deep call -> conditional -> jsx-sink` — 1 sink, max depth 9

Deepest member: `src/Chart.tsx:43`

```
-> props  [source]
-> tasks  [property-read]
-> groupBarSeries  [call]
-> barData  [call]
-> map  [call]
-> barSeries  [call]
-> props.type === "bar" ? «barSeries()» : lineSeries()  [conditional]
-> activeSeries  [call]
-> length  [property-read]
```

### `shallow conditional -> jsx-sink` — 3 sinks, max depth 3

Deepest member: `src/DashboardShell.tsx:106`

```
-> props  [source]
-> selected  [property-read]
-> «props.selected» ? "selected" : ""  [conditional]
```

### `shallow call -> jsx-sink` — 3 sinks, max depth 3

Deepest member: `src/DashboardShell.tsx:82`

```
-> props  [source]
-> tasks  [property-read]
-> map  [call]
```

### `medium jsx-sink` — 2 sinks, max depth 4

Deepest member: `src/Chart.tsx:73`

```
-> props  [source]
-> series  [property-read]
-> point  [iteration]
-> value  [property-read]
```

### `medium object-pack -> conditional -> jsx-sink` — 1 sink, max depth 5

Deepest member: `src/Chart.tsx:42`

```
-> props  [source]
-> type  [property-read]
-> «props.type» === "bar"  [conditional]
-> «props.type === "bar"» ? "Estimate by task" : "Cumulativ…  [conditional]
-> caption  [alias]
```


---

_Regenerate this report:_

```sh
tsx-dataflow --root examples/bad-ish-solid --view all --max-items 8 --out examples/bad-ish-solid/reports
```

_Focus on one file or region:_ append `--file <path|glob>` (repeatable) — e.g. `--file Button.tsx`, `--file src/components/Button.tsx`, or `--file 'src/dashboard/**'`. Combine with `--scope` to target a symbol within it.
