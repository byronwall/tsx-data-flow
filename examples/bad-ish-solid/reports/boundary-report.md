# Boundary Report

> Generated by **tsx-dataflow**, a static render-path data-flow analyzer for TypeScript/TSX (Solid/SolidStart-aware), from `examples/bad-ish-solid` on 2026-06-22. It parses the source and builds a graph from each value (props, signals, hooks, locals) through every transformation to the JSX **sink** that renders it, then ranks the heaviest render paths.
>
> First-party functions reached while tracing render paths, scored as data-flow boundaries. _In-src_ = distinct values reaching the return; _Callers_ = call sites across analyzed files; _Internal d/churn_ = depth/representation churn inside the body. _Verdict_ flags clean pipes, thin pass-throughs (inline), leaky boundaries, confluence/junctions, and hidden-mess internals.

| Function          | Where                 | Arity | In-src | Callers | Internal d/churn | Return type                             | Verdict         |
| ----------------- | --------------------- | ----- | ------ | ------- | ---------------- | --------------------------------------- | --------------- |
| taskRowView()     | src/view-models.ts:21 | 3     | 6      | 1       | 10/25            | `{ label: string; ownerLabel: string;…` | messy internals |
| buildRouteModel() | src/view-models.ts:6  | 1     | 6      | 1       | 8/9              | `{ actor: User; visibleTasks: Task[];…` | messy internals |

## Worst boundary debt

- **taskRowView** (messy internals) — internal depth 10, churn 25, 14 defensive op(s) behind a narrow signature.
- **buildRouteModel** (messy internals) — internal depth 8, churn 9, 7 defensive op(s) behind a narrow signature.

---

_Regenerate this report:_

```sh
tsx-dataflow --root examples/bad-ish-solid --view boundary-report --max-items 8 --out examples/bad-ish-solid/reports/boundary-report.md
```
