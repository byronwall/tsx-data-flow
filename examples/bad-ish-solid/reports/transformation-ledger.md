# Transformation Ledger

> Generated by **tsx-dataflow**, a static render-path data-flow analyzer for TypeScript/TSX (Solid/SolidStart-aware), from `examples/bad-ish-solid` on 2026-06-22. It parses the source and builds a graph from each value (props, signals, hooks, locals) through every transformation to the JSX **sink** that renders it, then ranks the heaviest render paths.
>
> Walks the single heaviest render path step by step. Each row is one transformation hop with its _Operation_ kind; the summary tallies semantic vs. representation-only vs. defensive steps along the path.

src/DashboardShell.tsx:110

| #   | Step                                                                                            | Operation     |
| --- | ----------------------------------------------------------------------------------------------- | ------------- |
| 1   | `props`                                                                                         | source        |
| 2   | `preferences`                                                                                   | property-read |
| 3   | `accentColor`                                                                                   | property-read |
| 4   | `«preferences.accentColor» ?? "#3f7f6f"`                                                        | fallback      |
| 5   | `…ask, owner: user, swatch: «preferences.accentColor ?? "#3f7f6f"», }`                          | object-pack   |
| 6   | `packed`                                                                                        | alias         |
| 7   | `task`                                                                                          | property-read |
| 8   | `estimateHours`                                                                                 | property-read |
| 9   | `«packed.task.estimateHours» ?? 0`                                                              | fallback      |
| 10  | `` `${«packed.task.estimateHours ?? 0»}h` ``                                                    | template      |
| 11  | ``… "normal", estimateLabel: «`${packed.task.estimateHours ?? 0}h`», swatch: packed.swatch, }`` | object-pack   |
| 12  | `taskRowView`                                                                                   | call          |
| 13  | `row`                                                                                           | alias         |
| 14  | `label`                                                                                         | property-read |
| 15  | `«row.label» ?? "Untitled"`                                                                     | fallback      |

## Summary

| Metric                    | Value |
| ------------------------- | ----- |
| semantic transformations  | 2     |
| representation-only steps | 26    |
| defensive steps           | 16    |
| total steps               | 15    |

---

_Regenerate this report:_

```sh
tsx-dataflow --root examples/bad-ish-solid --view all --max-items 8 --out examples/bad-ish-solid/reports
```
